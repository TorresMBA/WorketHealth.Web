@model Microsoft.AspNetCore.Identity.IdentityRole;
@{
    ViewData["Title"] = "Editar ROl";
}


<div class="clase-especial splash-container">
    <div class="card ">
        <div class="card-header" style="text-align:center">
            <h3 class="mb-1">Editar Rol</h3>
        </div>
        <div class="card-body">
            <form asp-controller="Roles" asp-action="Editar" method="post">
                <input type="hidden" asp-for="Id" />

                <div class="text-danger" asp-validation-summary="All"></div>

                <div class="form-group">
                    <input asp-for="Name" class="form-control form-control-lg" type="text" placeholder="Nombre del rol">
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <button type="submit" class="btn btn-primary btn-lg btn-block">Editar Rol</button>
            </form>
        </div>
    </div>
</div>


@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}